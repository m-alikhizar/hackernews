type Link {
	id: ID! @unique
	createdAt: DateTime!
	description: String!
	url: String!
	postedBy: User
	votes: [Vote!]!
}

type User {
	id: ID! @unique
	email: String! @unique
	name: String!
	password: String!
	links: [Link!]!
}

type Vote {
	id: ID! @unique
	link: Link!
	user: User!
}
